package metier;

public interface IMetier {
    double calcul();
    /* Pourquoi? meme principe : cest le contrat de la couche metier.
    La presentation ne connait que cette interface, pas l'implementattion concrete
     */
}
